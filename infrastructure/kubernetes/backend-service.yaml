apiVersion: v1
kind: Service
metadata:
  name: primecx-backend
  namespace: primecx
  labels:
    app: primecx-backend
    app.kubernetes.io/part-of: primecx
spec:
  type: ClusterIP
  selector:
    app: primecx-backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
